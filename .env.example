# Django Secret Key - Generate a new one using:
# uvx --from django django-admin shell -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# Django Debug Mode (set to False in production)
DEBUG=True

# Database Configuration (example for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/truth_food

# Allowed Hosts (comma-separated, for production)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
